steps:
  - name: "gcr.io/cloud-builders/docker"
    id: "Build"
    args:
      - "build"
      - "-t"
      - "gcr.io/$PROJECT_ID/Frontend:$SHORT_SHA"
      - "--build-arg"
      - "SIGOMS_USERS_LOGIN=${_SIGOMS_USERS_LOGIN}"
      - "."

  - name: "gcr.io/cloud-builders/docker"
    id: "Push"
    args:
      - "push"
      - "gcr.io/$PROJECT_ID/Frontend:$SHORT_SHA"

  - name: "gcr.io/cloud-builders/gcloud"
    id: "Deploy"
    args:
      - "run"
      - "deploy"
      - "Frontend"
      - "--image"
      - "gcr.io/$PROJECT_ID/Frontend:$SHORT_SHA"
      - "--platform"
      - "gke"
      - "--cluster"
      - "cluster-puc"
      - "--cluster-location"
      - "us-east1-b"
      - "--port"
      - "80"
      - "--namespace"
      - "sigomf-puc"
      - "--min-instances"
      - "1"
