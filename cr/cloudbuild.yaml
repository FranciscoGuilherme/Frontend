steps:
  - name: 'gcr.io/cloud-builders/yarn'
    args: ['install']

  - name: 'gcr.io/cloud-builders/yarn'
    args: ['run', 'build']
    env:
      - "SIGOMS_USERS_LOGIN=${_SIGOMS_USERS_LOGIN}"
      - "NODE_PATH=./src"